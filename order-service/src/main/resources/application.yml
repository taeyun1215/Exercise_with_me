server:
  port: 8085
  servlet:
    context-path: /order-service

spring:
  application:
    name: order-service
  profiles:
    active: dev
  datasource:
#    url: jdbc:mariadb://localhost:3306/order-db
    url: jdbc:mariadb://15.164.188.74:3306/order-db
    username: root
#    password: taeyun1215
    password: 6548

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  instance:
    instance-id: ${spring.cloud.client.hostname}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
#      defaultZone: http://localhost:8761/eureka
      defaultZone: http://ewm-eureka:8761/eureka

