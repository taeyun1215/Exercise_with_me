server:
  port: 8084
  servlet:
    context-path: /stock-service

spring:
  application:
    name: stock-service
  profiles:
    active: dev
  datasource:
#    url: jdbc:mariadb://localhost:3306/stock-db
    url: jdbc:mariadb://43.201.144.113:3306/stock-db
    username: root
#    password: taeyun1215 #localhost
    password: 6548

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  redis:
    host: ewm-redis
    port: 6379

eureka:
  instance:
    instance-id: ${spring.cloud.client.hostname}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
#      defaultZone: http://localhost:8761/eureka
      defaultZone: http://ewm-eureka:8761/eureka
